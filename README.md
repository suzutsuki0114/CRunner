# crunner
Cをいちいちコンパイルせずに実行するためのプログラム

## 使い方
```Terminal
$ crun --help
Description:
  Run a C program without compile

Usage:
  crun [Options] <File>

Options:
  --help, -h   Print this usage and exit
```
